pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'export PATH=/Users/Shared/Jenkins/apache-maven-3.6.2/bin:$PATH'
                sh 'cd backend'
                sh 'mvn clean install'
            }
        }
    }
}
